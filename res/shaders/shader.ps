#version 440 core

in vec2 TexCoords;

uniform sampler2D normalMap;

out vec4 FragColor;

void main() 
{
    vec3 color = texture(normalMap, TexCoords).rgb;
    
    FragColor = vec4(color, 1.0);
}